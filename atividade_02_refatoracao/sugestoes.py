# Trocar os nomes das variáveis que possuem o nome list
# O IF que calcular a idade de todos é possível melhorar
# Tem muitas variaveis nas funções
# Na Classe não foi criado nenhum método.

person = Person(data[10].split(' ')[0], data[10].split(' ')[len(data[10].split(' '))-1], datetime.strptime(data[8], '%d/%m/%Y').date())
# Tem uma função bem fácil para pegar o último elemento da lista!!!

print('teste')